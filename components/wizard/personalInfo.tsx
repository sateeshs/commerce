import { NextPage } from "next";
interface Props{
    formStep:any, nextFormStep: any
}
const PersonalInfo: NextPage<Props> = ({ formStep, nextFormStep }) =>{

    return(<></>)
}
export default PersonalInfo;